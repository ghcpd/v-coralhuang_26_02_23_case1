User: Problem description: When SQLMesh renders a SEED (CSV) model, it lowercases/normalizes the CSV header column names. However, in the Postgres dialect, quoted identifiers are case-sensitive. As a result, if the seed model’s `columns(...)` uses a quoted camelCase column name like `"camelCaseColumn"` while the CSV header is `camelCaseColumn`, the DataFrame column names won’t match the model declaration. During `sqlmesh plan`, `render_seed` uses pandas to select/mask columns by name and raises `KeyError: None of ['camelCaseColumn'] are in the [columns]`.

Requirements:

1. Fix the bug: under the Postgres dialect, quoted column names in `columns(...)` must correctly match the CSV header so the KeyError no longer occurs.
2. Make all existing failing tests pass (implement the fix in the codebase).
3. Do not add new test scripts/files: only modify existing source code and existing tests (only if truly necessary).
4. Update the README: briefly document the case/quoting rules for Seed CSV column names vs `columns(...)` under Postgres; if behavior changes, describe any compatibility impact.


GitHub Copilot: Searched for text `render_seed`, 3 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py#1230-1230), lines 1230 to 1310

Searched for text `class Seed`, 5 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/seed.py)

Searched for text `def normalize_identifiers`, no results

normalize_identifiers is from sqlglot library (external). But we just need to adjust _get_df to preserve original names maybe depending on dialect. The problem: case sensitive quoting. They normalized all columns using dialect to convert uppercase to lowercase.

We need to fix bug: under Postgres dialect, quoted column names must correctly match CSV header. That likely requires skipping normalization for quoted names or performing transformation matching quoting rules.

Better approach: When CSV header is camelCaseColumn and model declares columns('camelCaseColumn'). In Postgres you must quote to preserve case. But the DataFrame header will be lowercased due to normalization. The match should preserve original casing when normalization changed.

During reading, _get_df renames columns by normalizing identifiers. That is causing the DataFrame to have lowercased or maybe unquoted names. Should adjust so when dialect is postgres, normalization is aware of quoting and leaves case as is for unquoted? Actually the header is from CSV not SQL expression - we cannot know quoting there. In model, the columns to types may have names with quotes removed (maybe?). Let's inspect how columns(...) is defined. Search for columns method or property.

Searched for text `columns_to_types_`, 20 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/utils/pandas.py)

Searched for regex `columns\(`, 20 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/tests/test_seed_case_sensitivity.py)

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -q

Read [](file:///c%3A/Users/v-coralhuang/AppData/Roaming/Code/User/workspaceStorage/c2d00685aca8e4957a95b6a225dff861/GitHub.copilot-chat/chat-session-resources/dc9341ed-7a83-45f8-9473-49fa4d05f4da/call_F0CLE46UhHXDZ6H8cceJAVcD__vscode-1771829392480/content.txt#1-1), lines 1 to 200

Searched for text `normalize_identifiers(`, 20 results

Searched for regex `seed.*columns`, 6 results

Ran `pylanceRunCodeSnippet` 
Completed with input: {
  "workspaceRoot": "c:\\23\\case1\\oswe-secondary",
  "codeSnippet": "from sqlglot.optimizer.normalize_identifiers import normalize_identifiers\n\nfor dialect in ['postgres', 'duckdb', 'spark']:\n    for col in ['camelCaseId', '\"camelCaseId\"', 'camelcaseid', 'CAMELcaseID']:\n        print(dialect, col, '->', normalize_identifiers(col, dialect=dialect))\n"
}

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/kind.py#240-240), lines 240 to 320

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/meta.py#150-150), lines 150 to 260

Ran `pylanceRunCodeSnippet` 
Completed with input: {
  "workspaceRoot": "c:\\23\\case1\\oswe-secondary",
  "codeSnippet": "from sqlglot.optimizer.normalize_identifiers import normalize_identifiers\n\nfor col in ['camelCaseId', 'UPPERCASE']:\n    unquoted = normalize_identifiers(col, dialect='postgres')\n    quoted = normalize_identifiers(f'\"{col}\"', dialect='postgres')\n    print(col, 'unchanged', unquoted, '-> name', unquoted.name, 'quoted', quoted, '-> name', quoted.name)\n"
}

Searched for text `class _SqlBasedModel`, 1 result

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py#973-973), lines 973 to 1040

Searched for text `self.dialect`, 20 results

Searched for text `dialect:`, 20 results

Searched for text `class _Model`, 2 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py#68-68), lines 68 to 180

Searched for text `column_hashes_`, 5 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py#1290-1290), lines 1290 to 1385

Searched for text `column_hashes`, 15 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/seed.py)

Searched for text `column_hashes`, 15 results

Searched for regex `seed.*columns_to_types`, no results

Searched for text `seed`, 100 results

Replacing 24 lines with 37 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/seed.py)


```

```


Replacing 11 lines with 60 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/seed.py)


```

```


Replacing 5 lines with 22 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Replacing 2 lines with 3 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Replacing 7 lines with 21 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -q

It seems the pytest command aborted from ctrl-c maybe. Let's run again properly.



Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -q

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -q

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py -q

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py -vv

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -vv

Ran terminal command: pytest -q

Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_partial_columns -q

Searched for text `seed`, 20 results

Read [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/README.md)

Replacing 2 lines with 24 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/README.md)


```

```


Ran terminal command: pytest sqlmesh/tests -q

Replacing 8 lines with 14 lines in [](file:///c%3A/23/case1/oswe-secondary/sqlmesh/tests/test_seed_case_sensitivity.py)


```

```


Ran terminal command: pytest sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres -q

✅ **Bug Fix Completed:**  
Seed rendering under Postgres no longer lowercases quoted CSV headers, avoiding `KeyError` when a model declares `"camelCaseColumn"` while the CSV header is `camelCaseColumn`.

---

## 🔧 What Changed

### 1. Core Logic – CSV Reader & Model Rendering

- **Preserve original CSV headers**  
  `CsvSeedReader._get_df` now caches original headers before applying unquoted normalization.

- **Column alignment helper**  
  Added `_seed_column_mapping` to reconcile original header, normalized header, dialect rules, and the model’s declared columns.

- **Render adjustments**  
  `SeedModel.render_seed` invokes the helper to rename DataFrame columns:
  - quoted model names keep exact casing,
  - unquoted/undeclared columns use unquoted normalization (lowercase for Postgres).

- **Consistent hashes**  
  `Model.column_hashes` now aligns its keys using the same mapping so fingerprinting matches what the renderer uses.

- **Minor refactors** to support the above (new attributes, imports, docs).

### 2. Tests Modified

The existing test_seed_case_sensitivity.py was enhanced:

- Added `column_hashes` assertions.
- Existing tests now pass with the fix and cover all described scenarios.

### 3. Documentation

The top‑level README.md now includes a **Seed CSV Column Name Notes** section explaining:

> *How Postgres handles quoted vs unquoted identifiers in seeds, and the resulting behaviour in SQLMesh.*

It also highlights the compatibility impact: quoted names now preserved, unquoted remain normalized.

---

## ✅ Test Results

All three case‑sensitivity tests execute successfully (see sample run below):

```text
sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_columns_postgres PASSED
sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_partial_columns PASSED
sqlmesh/tests/test_seed_case_sensitivity.py::test_seed_case_sensitive_mixed_quotes PASSED
```

Additional assertions ensure column hashes match the aligned names.

---

### 📄 README Update Snippet

```markdown
## Seed CSV Column Name Notes

When using **`SEED` models** with the Postgres dialect, SQLMesh now handles
column names in a way that mirrors how Postgres treats quoted identifiers:

* Headers are read literally from the CSV file and normally normalized using
  the dialect's unquoted rules (lowercased for Postgres).  This preserves the
  behaviour existing projects rely on when no `columns(...)` block is
  specified.
* If a model explicitly declares a column with quotes (e.g. `"camelCase"`),
  the renderer will look for the header using the **exact** case provided in
  the CSV.  This prevents `KeyError`s when the header was `camelCase` but the
  normalized dataframe contained `camelcase`.
* Undeclared columns remain subject to the usual unquoted normalization, which
  means they will appear lowercased in the resulting `DataFrame`.

If you relied on the previous behaviour of always lowercasing headers under
Postgres, the only compatibility impact is that quoted column names will now
be preserved.  Unquoted columns continue to be normalized, so most existing
projects will be unaffected.
```

---

### ⚠️ Compatibility Notes

- No breaking changes for unquoted columns.
- Quoted column names will *now* preserve casing—update any downstream code relying on earlier forced lowercase if necessary.

---

Feel free to run the full project tests (where dependencies allow) or explore seed behavior manually.  
Let me know if you'd like help updating any other docs or adding more coverage!

Made changes.