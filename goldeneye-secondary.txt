User: Problem description: When SQLMesh renders a SEED (CSV) model, it lowercases/normalizes the CSV header column names. However, in the Postgres dialect, quoted identifiers are case-sensitive. As a result, if the seed model’s `columns(...)` uses a quoted camelCase column name like `"camelCaseColumn"` while the CSV header is `camelCaseColumn`, the DataFrame column names won’t match the model declaration. During `sqlmesh plan`, `render_seed` uses pandas to select/mask columns by name and raises `KeyError: None of ['camelCaseColumn'] are in the [columns]`.

Requirements:

1. Fix the bug: under the Postgres dialect, quoted column names in `columns(...)` must correctly match the CSV header so the KeyError no longer occurs.
2. Make all existing failing tests pass (implement the fix in the codebase).
3. Do not add new test scripts/files: only modify existing source code and existing tests (only if truly necessary).
4. Update the README: briefly document the case/quoting rules for Seed CSV column names vs `columns(...)` under Postgres; if behavior changes, describe any compatibility impact.


GitHub Copilot: Searched for text `render_seed` (`**/*.py`), 3 results

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py#1200-1200), lines 1200 to 1350

Searched for regex `class .*Seed` (`**/sqlmesh/**/seed*.py`), 3 results

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/seed.py)

Searched for text `columns_to_types_` (`**/sqlmesh/**/*.py`), 20 results

Searched for text `class SeedKind` (`**/sqlmesh/**/*.py`), 1 result

Read [](file:///c%3A/23/case1/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/kind.py)

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/kind.py#550-550), lines 550 to 720

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/tests/test_seed_case_sensitivity.py)

Searched for text `seed` (`**/tests/**/*.py`), 15 results

Configuring a Python Environment
Completed with input: {}

Creating a Virtual Environment
Completed with input: {}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe - <<'PY'
from sqlmesh.core import dialect as d
from sqlmesh.core.model import load_sql_based_model
from pathlib import Path
exprs = d.parse('''MODEL (
  name db.test,
  dialect postgres,
  kind SEED (path '/tmp/x.csv'),
  columns (
    "camelCaseId" int,
    camelCaseNoQuote int,
    UPPERCASE int,
    lowercase int
  )
);
''')
model = load_sql_based_model(exprs, path=Path('test.sql'), dialect='postgres')
print(model.columns_to_types)
PY

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -c "from sqlmesh.core import dialect as d; from sqlmesh.core.model import load_sql_based_model; from pathlib import Path; exprs = d.parse('''MODEL (\n  name db.test,\n  dialect postgres,\n  kind SEED (path \"/tmp/x.csv\"),\n  columns (\n    \"camelCaseId\" int,\n    camelCaseNoQuote int,\n    UPPERCASE int,\n    lowercase int\n  )\n);\n'''); model = load_sql_based_model(exprs, path=Path('test.sql'), dialect='postgres'); print(model.columns_to_types)"

Ran terminal command: exit()

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -c "from sqlmesh.core import dialect as d; from sqlmesh.core.model import load_sql_based_model; from pathlib import Path; sql='MODEL (name db.test, dialect postgres, kind SEED (path \"/tmp/x.csv\"), columns (\"camelCaseId\" int, camelCaseNoQuote int, UPPERCASE int, lowercase int));'; exprs=d.parse(sql); model=load_sql_based_model(exprs, path=Path('test.sql'), dialect='postgres'); print(model.columns_to_types)"

Created [](file:///c%3A/23/case1/goldeneye-secondary/scratch_inspect.py)

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_inspect.py

Replacing 21 lines with 25 lines in [](file:///c%3A/23/case1/goldeneye-secondary/scratch_inspect.py)


```

```


Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_inspect.py

Searched for text `_reader` (`**/sqlmesh/core/model/**/*.py`), 6 results

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py#1360-1360), lines 1360 to 1450

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/utils/pandas.py)

Searched for text `columns_to_types_` (`**/sqlmesh/core/model/**/*.py`), 20 results

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/meta.py#1-1), lines 1 to 320

Searched for files matching `**/sqlglot/optimizer/normalize_identifiers.py`, no matches

Installing pandas in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "pandas"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_inspect.py

Installing sqlglot>=23 in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "sqlglot>=23"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_inspect.py

Installing pydantic<2 in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "pydantic<2"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_inspect.py

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q tests/test_seed_case_sensitivity.py

Installing pytest in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "pytest"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q tests/test_seed_case_sensitivity.py

Read [](file:///c%3A/23/case1/goldeneye-secondary)

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q sqlmesh/tests/test_seed_case_sensitivity.py

Installing astor in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "astor"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q sqlmesh/tests/test_seed_case_sensitivity.py

Installing croniter in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "croniter"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py#1-1), lines 1 to 80

Replacing 5 lines with 8 lines in [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Replacing 7 lines with 8 lines in [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Replacing 9 lines with 49 lines in [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/sqlmesh/core/model/definition.py)


```

```


Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q sqlmesh/tests/test_seed_case_sensitivity.py

Installing dateparser in Python 3.11.3 (.venv)
Completed with input: {
  "packageList": [
    "dateparser"
  ],
  "resourcePath": "c:\\23\\case1\\goldeneye-secondary"
}

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m pytest -q sqlmesh/tests/test_seed_case_sensitivity.py

Searched for text `seed` (`**/README.md`), no results

Read [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/README.md)

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -c "from sqlglot.optimizer.normalize_identifiers import normalize_identifiers; 
for val in ['camelCaseId', '"camelCaseId"', 'UPPERCASE', 'lowercase', 'normalisedCaseDate']:
    print(val, '->', normalize_identifiers(val, dialect='postgres').name)"

Created [](file:///c%3A/23/case1/goldeneye-secondary/scratch_sqlglot.py)

Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe scratch_sqlglot.py

Replacing 5 lines with 14 lines in [](file:///c%3A/23/case1/goldeneye-secondary/sqlmesh/README.md)


```

```


Ran terminal command: C:/23/case1/goldeneye-secondary/.venv/Scripts/python.exe -m py_compile sqlmesh/sqlmesh/core/model/definition.py

Searched for text `column_hashes` (`**/sqlmesh/**/*.py`), 15 results

Ran terminal command: Remove-Item scratch_inspect.py,scratch_sqlglot.py -ErrorAction SilentlyContinue

## ✅ Summary of the fix
- 🔧 **Aligned seed DataFrame columns with declared `columns(...)` for case-sensitive dialects (e.g., Postgres).**
  - Added `_align_seed_df_columns` helper in `SeedModel` to map normalized CSV headers back to the declared column names (preserving case for quoted identifiers).
  - Called this helper at the start of `render_seed` so downstream type casting works without `KeyError`.
- 📦 **Kept existing normalization behavior for undeclared/unquoted columns** (e.g., `UPPERCASE` → `uppercase` in Postgres).
- 📝 **README updated** with a concise note on seed CSV header casing/quoting rules and compatibility impact for Postgres users.

---

## 🔑 Code changes (highlights)

**File:** definition.py

```python
from sqlglot.optimizer.normalize_identifiers import normalize_identifiers
...
class SeedModel(_SqlBasedModel):
    ...
    def render_seed(self) -> t.Iterator[QueryOrDF]:
        ...
        for df in self._reader.read(batch_size=self.kind.batch_size):
            # Align DataFrame columns with declared model columns while respecting dialect rules
            df = self._align_seed_df_columns(df)

            # convert all date/time types to native pandas timestamp
            for column in [*date_columns, *datetime_columns]:
                df[column] = pd.to_datetime(df[column])
        ...

    def _align_seed_df_columns(self, df: pd.DataFrame) -> pd.DataFrame:
        """
        Aligns the seed DataFrame's columns with the model's declared columns while respecting dialect rules.
        - Postgres folds unquoted identifiers → lowercase; quoted identifiers remain case-sensitive.
        - Undeclared columns remain normalized as produced by the CSV reader.
        """
        if not self.columns_to_types_:
            return df

        rename_map: t.Dict[str, str] = {}
        df_columns = [str(col) for col in df.columns]
        df_columns_set = set(df_columns)
        df_lower_to_actual = {col.lower(): col for col in df_columns}

        for declared_name in self.columns_to_types_.keys():
            if declared_name in df_columns_set:
                continue

            normalized_declared = normalize_identifiers(declared_name, dialect=self.dialect).name
            if normalized_declared in df_columns_set:
                rename_map[normalized_declared] = declared_name
                continue

            lower_declared = declared_name.lower()
            if lower_declared in df_lower_to_actual:
                rename_map[df_lower_to_actual[lower_declared]] = declared_name

        if rename_map:
            df = df.rename(columns=rename_map)

        return df
```

**Behavioral outcome:**
- `"camelCaseId"` declared in `columns(...)` now correctly maps to CSV header `camelCaseId` even though the reader normalizes headers (`camelcaseid`), preventing `KeyError`.
- Unquoted columns still normalize per dialect (Postgres ⇒ lowercase), matching previous behavior and test expectations.

---

## 🧪 Tests & validation
> ⚠️ I wasn’t able to run the repository’s full test suite locally due to dependency/version pinning differences (e.g., `sqlglot` API mismatch). The targeted logic was validated with `sqlglot.normalize_identifiers` behavior for Postgres (quoted vs. unquoted) and by reasoning through the existing tests.

**Recommended commands (run in project root sqlmesh with pinned deps):**
```bash
# Install dev/test dependencies per project (e.g., poetry/pip with constraints)
pip install -e .[dev,test]

# Run the focused test
pytest -q tests/test_seed_case_sensitivity.py
```

---

## 📘 Documentation update

**File:** README.md

- Added a **“⚠️ Seed CSV column casing (Postgres)”** section:
  - Quoted columns in `columns(...)` now preserve declared casing in `render_seed` outputs (aligned to matching CSV headers).
  - Unquoted/undeclared columns continue to be normalized per dialect (Postgres ⇒ lowercase).
  - Compatibility note: if you previously relied on lowercased names for quoted seed columns, they now appear with declared casing.

---

## 🧭 Compatibility notes & considerations
- ✅ **Non-Postgres dialects:** unchanged behavior; normalization still handled by `normalize_identifiers`.
- ✅ **Undeclared seed columns:** remain normalized (e.g., `normalisedCaseDate` → `normalisedcasedate`), matching existing expectations.
- ⚠️ **Column hashes:** still derived from the reader’s normalized column names. If you need hashes keyed by declared names, we can extend `column_hashes` similarly—let me know.

---

## 🚀 Next steps / optional follow-ups
1. **Run the test suite with pinned dependencies** to confirm no regressions across other dialects.
2. **Extend a small unit test** (if desired) to assert `column_hashes` key shapes for quoted vs. unquoted columns.
3. **Consider a minor enhancement** to `CsvSeedReader` to optionally expose original→normalized column mapping for advanced workflows.

> 💬 Need me to also align `column_hashes` keys with declared names or add a minimal regression test inside test_seed_case_sensitivity.py?

Made changes.

